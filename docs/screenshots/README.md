# Orion Screenshot Inventory

These screenshots are used in README/demo walkthroughs and should reflect the current local UI state.

## Current files
- `workflow-builder.png`
- `dag-view.png`
- `run-logs.png`
- `dashboard-metrics.png`
- `approval-queue.png`
- `agent-monitor.png`
- `api-docs.png`

## What each image shows
- **workflow-builder.png** — workflow setup form and goal input.
- **dag-view.png** — dependency graph for planned steps.
- **run-logs.png** — step-by-step execution history and traces.
- **dashboard-metrics.png** — KPI/summary cards for runs and outcomes.
- **approval-queue.png** — sensitive step paused pending reviewer decision.
- **agent-monitor.png** — planner/worker/reviewer plus tool health snapshots.
- **api-docs.png** — FastAPI OpenAPI page (`/docs`) for endpoint walkthrough.

## Refresh checklist (when UI changes)
1. Start backend + frontend locally.
2. Re-capture the same screens with the same filenames.
3. Confirm images still match current labels and agent behavior (stub-based).

## TODOs
- Add a dated capture note for each screenshot (example: `Captured: 2026-05-12`).
- Add one mobile-width screenshot to show responsive behavior.
